---
import Footer from "../components/Footer.astro";
import Navbar from "../components/Navbar.astro";
import Menubar from "../components/Menubar.astro";
import { GoogleFontsOptimizer } from "astro-google-fonts-optimizer";
import RotateDevice from "../components/RotateDevice.astro";
import ModalCertif from "../components/ModalCertif.astro";
var dev = true;
---

<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link rel="stylesheet" href="/globalstyle.css" />
    <GoogleFontsOptimizer
      url="https://fonts.googleapis.com/css2?family=Allura&display=swap"
    />
    <GoogleFontsOptimizer
      url="https://fonts.googleapis.com/css2?family=Nunito+Sans:opsz,wght@6..12,200&display=swap"
    />
    <script
      type="module"
      type="text/javascript"
      src="https://cdnjs.cloudflare.com/ajax/libs/ionicons/5.1.2/ionicons/ionicons.esm.js"
    ></script>
    <title>Hypnose Paris</title>
  </head>
  <!-- Google tag (gtag.js) --> 
  <script async src="https://www.googletagmanager.com/gtag/js?id=G-WP6XX5L4TN"></script> <script> window.dataLayer = window.dataLayer || []; function gtag(){dataLayer.push(arguments);} gtag('js', new Date()); gtag('config', 'G-WP6XX5L4TN'); </script>
  <body>
    <div style="width:100vw;margin:0 auto;max-width: 1280px;">
      <Navbar />
    </div>

    <div class="maincontent">
      <slot />
    </div>

    <Footer />

    <ModalCertif />


   <style>
    .maincontent {
      max-width: 1280px; 
        margin:0 auto;
        margin-top:10rem;
        transform:scale(1.05);
        background:white;
        box-shadow: 0px 3px 15px rgba(0,0,0,0.2);
    }


    @media only screen and (max-width: 430px) {
      .maincontent {
        margin-top:0rem;
        transform:scale(1.0);
      }
    }
   </style>


    <a id="mailer"></a>
    {
      ({ dev } && <script is:inline src="/js/dest.js" />) || (
        <script is:inline src="/js/destobfus.js" />
      )
    }

    {{ dev } && console.log("IN DEV")}

    <script is:inline>
      window.sendMail = function (target = "mailer") {
        document
          .getElementById(target)
          ?.setAttribute(
            "href",
            `mailto:${destx()}?subject=Demande%20de%20Rendez-vous`
          );
        document.getElementById(target)?.click();
      };
    </script>
    <script
      src="https://static.elfsight.com/platform/platform.js?onload=test"
      data-use-service-core
      defer></script>
    <div id="bc">
      <div class="elfsight-app-d1095e31-2a31-47b0-9022-67301b69e2cf"></div>
    </div>

    <script is:inline>
      const observer = new IntersectionObserver(
        ([e]) => {
   
        },
        {
          threshold: [1],
        }
      );

      setTimeout(() => {
        const stickyElm = document.querySelector(".kyAUVn");
        const svggoog = document.querySelector(".es-badge-sources-container")
        const reviews = document.querySelector(".es-badge-total-reviews");
        observer.observe(stickyElm);
        //svggoog.style.display = "none";
        //reviews.style.display = "none";
        stickyElm.style = `
          position: fixed;
          
          bottom: -0.6rem;
          left: -0.99rem;
          z-index:1000;
        `;
      }, 5000);
    </script>
    <script>
      console.log("TEST")
    </script>
  </body>
</html>
